server:
  port: 8080
  servlet:
    context-path: /v1/api

spring:
  application:
    name: wish-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.100.213.205:3306/signin?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: Mydb6.
  devtools:
    restart:
      enabled: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

mybatis:
  configuration:
    map-underscore-to-camel-case: true

